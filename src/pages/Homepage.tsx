import React from "react";

type homeProps ={
  name: string
}

export default function Homepage({name}: homeProps){
  return (
    <div>
      <h1>{name}</h1>
    </div>
    // Quick about us
    // Most recent news
    // Sign up now
    // Testimonies
    //
  );
};
